
%% 2014.09.30 - Notes on warmabs_vs_xi_test.sl (written by Dave)
%% Author: lia@space.mit.edu

Requires warmabs_db

FUNCTION run_models( info )
INPUT    info = struct{ mname, pname, min, max, step }
USE      Steps through parameter (pname) of an XSTAR model (mname)

FUNCTION mk_pfiles( info )
INPUT    info = struct{ mname, pname, min, max, step }
USE      Steps through parameter (pname) of XSTAR model (mname) and saves parameter values to a file.

FUNCTION xstar_collate_trans -- "Obsolete function... see xstar_collate_trans2()"

FUNCTION (tmax_line, tmax_edge) = xstar_get_max_trans( t )
INPUT    t = Struct_Type[]
USE      Returns the maximum transition id number for type="line" and type="edge/rrc" transitions

??
FUNCTION xstar_add_unique_id_field( t )
INPUT    t = Struct_Type[]
USE      Adds the "uid" (unique transition id) to t. The uid is
         an integer array with the same length as each respective db.

FUNCTION xstar_collate_trans2 ( t )
INPUT    t = Struct_Type[]
USE      Assigns the unique id (t[i].transition if type=="line",
         t[i].transition + tmax_line if type=="edge/rrc") for each db in t.

FUNCTION t = _xstar_load_tables( fnames )
INPUT    fnames = String_Type[]
USE      Returns an array of db structures from an array of filenames

?? 
FUNCTION xstar_get_table_param( t, s )
INPUT    t = Struct_Type[]; s : a db structure
USE      Returns the value of parameter s

??
FUNCTION xstar_collect_params( t, s )
INPUT    t : , s :

FUNCTION xstar_collate_trans_pointers( t )
INPUT    t
USE      